{{>layout/header}}

<div class="container mt-5">
    <h2>📦 나의 주문 내역</h2>
    <hr>

    <table class="table table-hover mt-4">
        <thead class="table-light">
        <tr>
            <th>주문번호</th>
            <th>상품 사진</th>
            <th>상품명</th>
            <th>브랜드</th>
            <th>주문 일시</th>
        </tr>
        </thead>
        <tbody>
        {{#orders}}
            <tr>
                <td>{{id}}</td>
                <td>
                    <img src="{{item.imgUrl}}" width="60" height="60" style="object-fit: cover; border-radius: 5px;">
                </td>
                <td class="fw-bold">{{item.name}}</td>
                <td>{{item.brand}}</td>
                <td>{{orderDate}}</td>
            </tr>
        {{/orders}}

        {{^orders}}
            <tr>
                <td colspan="5" class="text-center py-5">
                    아직 주문 내역이 없습니다. 쇼핑을 즐겨보세요!
                </td>
            </tr>
        {{/orders}}
        </tbody>
    </table>

    <div class="mt-4 text-center">
        <a href="/" class="btn btn-primary">쇼핑 계속하기</a>
    </div>
</div>

{{>layout/footer}}