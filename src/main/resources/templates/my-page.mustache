{{>layout/header}}

<div class="container mt-5" style="max-width: 900px;">
    <div class="card border-0 shadow-sm rounded-4 p-4 mb-4 bg-light">
        <div class="d-flex align-items-center">
            <div class="bg-white rounded-circle d-flex align-items-center justify-content-center shadow-sm me-4" style="width: 80px; height: 80px;">
                <i class="fas fa-user fs-1 text-secondary"></i>
            </div>
            <div>
                <h3 class="fw-bold mb-1">{{userName}}님</h3>
                <p class="text-muted mb-0">{{email}}</p>
            </div>
            <div class="ms-auto text-end">
                <a href="/my-info" class="btn btn-outline-dark btn-sm rounded-pill px-3">내 정보 수정 ></a>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm rounded-4 h-100">
                <div class="card-header bg-white border-0 fw-bold pt-4 px-4 d-flex justify-content-between">
                    <span>📦 최근 주문 내역</span>
                    <a href="/my-orders" class="text-decoration-none small text-muted">더보기 ></a>
                </div>
                <div class="card-body px-4 pb-4">
                    {{#recentOrders}}
                        <div class="d-flex align-items-center py-3 border-bottom">
                            <img src="{{item.imgUrl}}" class="rounded me-3" width="50" height="50" style="object-fit: cover;">
                            <div class="flex-grow-1">
                                <h6 class="mb-0 fw-bold small">{{item.name}}</h6>
                                <small class="text-muted">{{orderDate}}</small>
                            </div>
                            <span class="badge bg-secondary">{{status}}</span>
                        </div>
                    {{/recentOrders}}
                    {{^recentOrders}}
                        <div class="text-center py-4 text-muted small">최근 주문 내역이 없습니다.</div>
                    {{/recentOrders}}
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card border-0 shadow-sm rounded-4 h-100">
                <div class="card-header bg-white border-0 fw-bold pt-4 px-4">QUICK MENU</div>
                <div class="card-body px-4">
                    <div class="list-group list-group-flush">
                        <a href="/my-orders" class="list-group-item list-group-item-action border-0 px-0 py-3">
                            <i class="fas fa-undo me-2 text-warning"></i> 취소 / 반품 / 교환 내역
                        </a>

                        <a href="/payment" class="list-group-item list-group-item-action border-0 px-0 py-3">
                            <i class="far fa-credit-card me-2 text-primary"></i> 결제수단 관리
                        </a>

                        <a href="/wishlist" class="list-group-item list-group-item-action border-0 px-0 py-3">
                            <i class="far fa-heart me-2 text-danger"></i> 찜한 상품
                        </a>
                        <a href="/coupon" class="list-group-item list-group-item-action border-0 px-0 py-3">
                            <i class="fas fa-ticket-alt me-2 text-success"></i> 내 쿠폰함
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-5 mb-5">
        <h5 class="fw-bold mb-3">👀 최근 본 상품</h5>
        <div class="row row-cols-2 row-cols-md-5 g-3">
            {{#recentItems}}
                <div class="col">
                    <a href="/item/{{id}}" class="text-decoration-none text-dark">
                        <div class="card h-100 border-0 shadow-sm">
                            <img src="{{imgUrl}}" class="card-img-top" style="height: 150px; object-fit: cover;">
                            <div class="card-body p-2 text-center">
                                <small class="d-block text-truncate fw-bold">{{name}}</small>
                                <small class="text-danger">{{price}}원</small>
                            </div>
                        </div>
                    </a>
                </div>
            {{/recentItems}}
            {{^recentItems}}
                <div class="col-12 text-center py-5 text-muted bg-light rounded-3">
                    최근 본 상품이 없습니다.
                </div>
            {{/recentItems}}
        </div>
    </div>
</div>

{{>layout/footer}}