{{>layout/header}}

<div class="container mt-5" style="padding-top: 20px;">

    <div class="row mb-5">
        <div class="col-md-6 mb-4">
            <div class="position-relative">
                <img src="{{item.imgUrl}}" class="img-fluid rounded-4 shadow-sm" alt="{{item.name}}" style="width: 100%; object-fit: cover;">

                {{#item.isEvent}}
                    <span class="position-absolute top-0 start-0 m-3 badge bg-danger fs-6 px-3 py-2 rounded-pill shadow-sm">
                    EVENT 특가
                </span>
                {{/item.isEvent}}
            </div>
        </div>

        <div class="col-md-6">
            <div class="d-flex justify-content-between align-items-center">
                <span class="badge bg-dark px-3 py-2 rounded-pill">{{item.brand}}</span>
                <span class="text-muted small">상품번호 {{item.id}}</span>
            </div>

            <h2 class="fw-bold mt-3" style="word-break: keep-all;">{{item.name}}</h2>

            <hr class="my-4">

            {{#item.isDiscounted}}
                <div class="mb-4">
                    <span class="text-decoration-line-through text-muted fs-5 me-2">{{item.price}}원</span>
                    <span class="badge bg-danger fs-6 mb-1">{{item.discountRate}}% 할인</span>
                    <h1 class="fw-bold text-danger mt-1">{{item.salePrice}}원</h1>
                </div>
            {{/item.isDiscounted}}

            {{^item.isDiscounted}}
                <h3 class="fw-bold mb-4 text-dark">{{item.price}}원</h3>
            {{/item.isDiscounted}}


            {{#isAdmin}}
                <div class="card border-danger shadow-sm mb-3">
                    <div class="card-header bg-danger text-white fw-bold">
                        <i class="fas fa-tools me-2"></i> 관리자 상품 수정
                    </div>
                    <div class="card-body bg-light">
                        <form action="/admin/item/update" method="post">
                            <input type="hidden" name="itemId" value="{{item.id}}">

                            <div class="mb-2">
                                <label class="form-label small fw-bold text-danger">카테고리</label>
                                <select class="form-select form-select-sm" name="category">
                                    <option value="OUTER" {{#item.isOuter}}selected{{/item.isOuter}}>아우터</option>
                                    <option value="TOP" {{#item.isTop}}selected{{/item.isTop}}>상의</option>
                                    <option value="BOTTOM" {{#item.isBottom}}selected{{/item.isBottom}}>하의</option>
                                    <option value="SHOES" {{#item.isShoes}}selected{{/item.isShoes}}>신발</option>
                                </select>
                            </div>

                            <div class="mb-2">
                                <label class="form-label small fw-bold">상품명</label>
                                <input type="text" name="name" class="form-control form-control-sm" value="{{item.name}}">
                            </div>

                            <div class="mb-2">
                                <label class="form-label small fw-bold">가격 (콤마 포함)</label>
                                <input type="text" name="price" class="form-control form-control-sm" value="{{item.price}}">
                            </div>

                            <div class="mb-3">
                                <label class="form-label small fw-bold">이미지 URL</label>
                                <input type="text" name="imgUrl" class="form-control form-control-sm" value="{{item.imgUrl}}">
                            </div>

                            <button type="submit" class="btn btn-danger w-100 fw-bold btn-sm">
                                정보 수정 완료 (저장)
                            </button>
                        </form>

                        <div class="text-center mt-2">
                            <small class="text-muted">* 관리자는 구매할 수 없습니다.</small>
                        </div>
                    </div>
                </div>
            {{/isAdmin}}


            {{^isAdmin}}
                <div class="card bg-light border-0 p-4 rounded-4">
                    <form action="/cart/add" method="post">
                        <input type="hidden" name="itemId" value="{{item.id}}">

                        <div class="d-flex align-items-center mb-4">
                            <label class="form-label fw-bold me-3 mb-0">수량</label>
                            <input type="number" name="count" class="form-control border-0 shadow-sm text-center bg-white" value="1" min="1" max="10" style="width: 80px;">
                        </div>

                        <div class="d-grid gap-2 d-md-flex">
                            <button class="btn btn-dark btn-lg rounded-3 py-3 flex-grow-1 fw-bold" type="submit">🛒 장바구니 담기</button>
                        </div>
                    </form>

                    <form action="/wish/{{item.id}}" method="post" class="mt-2">
                        <div class="d-grid">
                            {{#isWished}}
                                <button class="btn btn-outline-danger btn-lg rounded-3 py-3 fw-bold" type="submit">❤️ 찜 취소</button>
                            {{/isWished}}
                            {{^isWished}}
                                <button class="btn btn-outline-danger btn-lg rounded-3 py-3 fw-bold" type="submit">🤍 찜하기</button>
                            {{/isWished}}
                        </div>
                    </form>
                </div>
            {{/isAdmin}}

            <div class="mt-4 d-flex align-items-center bg-light p-3 rounded-3">
                <div class="fs-1 me-3">🚚</div>
                <div>
                    <p class="mb-0 fw-bold">무료 배송 / 내일 도착 보장</p>
                    <small class="text-muted">지금 주문하면 내일 오후 2시 전 도착 예정입니다.</small>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-5 mb-5">
        <div class="col-12">
            <h3 class="fw-bold mb-4 border-bottom pb-3">⭐ 구매 고객 리뷰 <span class="text-muted fs-5">({{reviewCount}})</span></h3>

            <div class="card border-0 bg-light rounded-4 p-4 mb-5">
                <div class="row align-items-center">
                    <div class="col-md-4 text-center border-end">
                        <h1 class="display-3 fw-bold mb-0 text-dark">{{averageScore}}</h1>
                        <div class="text-warning fs-4 mb-2">⭐⭐⭐⭐⭐</div>
                        <p class="text-muted small mb-0">구매 만족도</p>
                    </div>
                    <div class="col-md-8 px-4">
                        <div class="d-flex align-items-center mb-2"><span class="small fw-bold me-2" style="width: 30px;">5점</span><div class="progress flex-grow-1" style="height: 8px;"><div class="progress-bar bg-warning" style="width: {{per5}}%"></div></div><span class="small text-muted ms-2">{{score5}}</span></div>
                        <div class="d-flex align-items-center mb-2"><span class="small fw-bold me-2" style="width: 30px;">4점</span><div class="progress flex-grow-1" style="height: 8px;"><div class="progress-bar bg-warning" style="width: {{per4}}%"></div></div><span class="small text-muted ms-2">{{score4}}</span></div>
                        <div class="d-flex align-items-center mb-2"><span class="small fw-bold me-2" style="width: 30px;">3점</span><div class="progress flex-grow-1" style="height: 8px;"><div class="progress-bar bg-warning" style="width: {{per3}}%"></div></div><span class="small text-muted ms-2">{{score3}}</span></div>
                        <div class="d-flex align-items-center mb-2"><span class="small fw-bold me-2" style="width: 30px;">2점</span><div class="progress flex-grow-1" style="height: 8px;"><div class="progress-bar bg-warning" style="width: {{per2}}%"></div></div><span class="small text-muted ms-2">{{score2}}</span></div>
                        <div class="d-flex align-items-center mb-2"><span class="small fw-bold me-2" style="width: 30px;">1점</span><div class="progress flex-grow-1" style="height: 8px;"><div class="progress-bar bg-warning" style="width: {{per1}}%"></div></div><span class="small text-muted ms-2">{{score1}}</span></div>
                    </div>
                </div>
            </div>

            <div class="row g-4">
                {{#reviews}}
                    <div class="col-md-6">
                        <div class="card border-0 shadow-sm h-100 rounded-4">
                            <div class="card-body p-4">
                                <div class="d-flex justify-content-between align-items-center mb-3">
                                    <div>
                                        <div class="text-warning fs-5" style="letter-spacing: -3px;">{{starDisplay}}</div>
                                        <small class="text-muted" style="font-size: 12px;">{{createdDate}}</small>
                                    </div>
                                    <div class="d-flex align-items-center">
                                        <div class="bg-light rounded-circle d-flex align-items-center justify-content-center me-2" style="width: 40px; height: 40px;">
                                            <i class="fas fa-user text-secondary"></i>
                                        </div>
                                        <span class="fw-bold small">{{member.name}}</span>
                                    </div>
                                </div>
                                <p class="card-text text-secondary" style="line-height: 1.6;">{{content}}</p>
                            </div>
                        </div>
                    </div>
                {{/reviews}}

                {{^reviews}}
                    <div class="col-12">
                        <div class="text-center py-5 bg-light rounded-4">
                            <div class="fs-1 mb-3 text-muted">📝</div>
                            <h5 class="text-muted">아직 등록된 리뷰가 없습니다.</h5>
                            <p class="text-secondary small">첫 번째 리뷰의 주인공이 되어보세요!</p>
                        </div>
                    </div>
                {{/reviews}}
            </div>
        </div>
    </div>

</div>

{{>layout/footer}}